export const metadata = {
  title: "9Router API Docs",
  description: "Interactive API reference for 9Router's OpenAI-compatible endpoints.",
};

export default function DocsLayout({ children }) {
  return children;
}
